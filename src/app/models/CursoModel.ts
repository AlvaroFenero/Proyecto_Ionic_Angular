export interface CursoModel {
    cod_curso: string;
    nombre_curso: string;
    numrun: string; //viene de la tabla usuario
    cod_clase: string; //viene de la tabla clase
    sala: string; //viene de la tabla clase
  }